var fs = require('fs');var fileName = '/Users/himanshu/development/nodeJs/package.json';fs.stat(fileName, function(err, state){	if(err){		console.error(err);	} else {		if(state.size > 1024){			fs.readFile(fileName, 'utf8', function(err, fileContent){				if(err){					console.error(err);				} else {					console.log("FileContent: " + fileContent);				}			});		}		console.log('file size is less then 1024');	}});