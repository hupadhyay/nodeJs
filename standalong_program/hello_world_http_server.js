var http = require('http');var server = http.createServer();var serviceMethod = function(req, res){	res.writeHead(200, {'content-type': 'text/plain'});	res.write("Hello World from Server");	res.end();}server.on('request', serviceMethod);var port = 9090;server.listen(port);var listenMethod = function(){	console.log('Hello world server listener on port %d', port);}server.once('listening', listenMethod);